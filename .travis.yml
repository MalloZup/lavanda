language: bash
sudo: required

# libssh-dev removed
before_install:
  - sudo apt-get -qq update
  - sudo apt-get install -y libssh-de ruby-dev python-dev 
  - sudo  grep -R 'ssh_event' /usr/include/libssh/
  - git clone https://github.com/openSUSE/twopence.git
  - cd twopence
  - make
  - sudo make install
  - sudo ldconfig
script:
    - ruby test/setupTesting.rb
    - ruby test/lavBasicTest.rb
